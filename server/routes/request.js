const express = require('express');

const requestRouter = express.Router();
const Request = require('../models/Request');


requestRouter.post("/new", (req, res, next) => {

})

module.exports = requestRouter;